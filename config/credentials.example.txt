# AppMate config — copy this file to config/credentials.txt and fill in.
# config/credentials.txt and all .p8/.pem keys in config/ are gitignored.
#
# ── SECURITY: API key role selection ──────────────────────────────────────
# AppMate is a READ-ONLY analytics tool. The key you reference here must NOT
# grant write access to App Store data, builds, or banking. When generating
# the key in App Store Connect → Users and Access → Integrations → App Store
# Connect API, check ONLY these roles:
#
#     [x] Sales and Reports
#     [x] Customer Support
#     [x] Marketing
#
# DO NOT check Admin / Developer / App Manager / Finance — those allow
# writing live App Store data and AppMate will refuse to start if it detects
# any of them via its role probe (see scripts/key_safety.py).

# --- App Store Connect API (required) ---
issuer_id        =
key_id           =
private_key_path = config/AuthKey_XXXXXXXX.p8
vendor_number    =

# --- AppMate RAG API (optional; defaults to public BETA endpoint) ---
rag_base_url     = https://appmate.000ooo.ooo
