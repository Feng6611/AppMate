---
description: Deep ASO optimization for one app — produce new title, subtitle, and keyword strings.
---

Run the AppMate ASO optimization workflow for the app: **$ARGUMENTS**

Invoke the `aso-optimize` skill and follow it end-to-end, starting with `python3 scripts/aso_optimize_v2.py analyze "$ARGUMENTS"`. If no app was given, ask the user which app to optimize (accepts App Store ID / bundle ID / SKU / fuzzy name).
